[Unit]
Description=Log Collector Daemon
After=network.target

[Service]
User={USER}
WorkingDirectory={PROJECT_DIR}
ExecStart={PROJECT_DIR}/venv/bin/python {PROJECT_DIR}/log_collector_daemon.py --log-file "{LOG_FILE_PATH}"
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
